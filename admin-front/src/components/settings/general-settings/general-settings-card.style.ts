import styled from "styled-components";
import { SPACES } from "../../../ui-constants/sizes";

export const GeneralSettingsCardContainer = styled.div`
  max-width: 800px;
  margin-top: ${SPACES.px30};
`;
