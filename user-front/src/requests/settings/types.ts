export interface IServicesConfig {
  donations?: boolean;
  employees?: boolean;
  volunteers?: boolean;
  projects?: boolean;
  financialControl?: boolean;
  marketing?: boolean;
  reports?: boolean;
  invoices?: boolean;
}

export interface IDetailsConfig {
  name?: string;
}
